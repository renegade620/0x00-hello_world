#include "main.h"

/**
 * _abs - check description
 *
 * @x: input integer
 *
 * Description: absolute value of an integer
 *
 * Return:  returns absolute value
 */
int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (x * -1);
}
