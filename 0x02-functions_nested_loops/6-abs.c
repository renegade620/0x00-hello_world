#include "main.h"

/**
 * _abs - check description
 *
 * @x - input integer
 *
 * Description: absolute value of an integer
 *
 * Return: absolute value
 */
int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (r * -1);
}
